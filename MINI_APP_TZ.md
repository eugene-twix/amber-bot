# Техническое Задание: Telegram Mini App для Amber Bot

## 1. Цель и Общая Концепция

Создание Telegram Mini App на базе React для Amber Bot, предоставляющего полный функционал существующего Telegram-бота через графический интерфейс.

**Ключевые решения:**
-   **Монорепозиторий:** Frontend (React) и Backend (Go) будут находиться в одном репозитории.
-   **API:** Взаимодействие Frontend и Backend будет осуществляться через REST API.
-   **Полный функционал:** Mini App будет включать в себя все текущие возможности бота, так как они составляют его ядро.

## 2. Структура Проекта (Monorepo)

-   **Backend (Go):** Остаётся в корне проекта.
-   **Frontend (React):** Размещается в новой директории `./frontend` в корне репозитория.

```
.
├── cmd/
├── internal/
├── frontend/             # Директория для React Mini App
│   ├── public/
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   └── main.tsx
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
├── docker-compose.yml
├── go.mod
├── Makefile
├── README.md
└── MINI_APP_TZ.md        # Этот файл
```

## 3. Технологический Стек

### Backend (Go)
-   **Язык:** Go
-   **API:** RESTful API
-   **Роутер:** Gin
-   **Статика:** Стандартный `http.FileServer` для отдачи `index.html` и статических ассетов из `frontend/dist`. SPA-fallback на `index.html` для клиентских роутов.
-   **Аутентификация:** Middleware для валидации Telegram `initData` и определения `telegram_id` и роли пользователя.
-   **Автосоздание пользователя:** При первом запросе, если пользователь не найден в БД, автоматически создаётся с ролью `viewer`.

### Frontend (React Mini App)
-   **Фреймворк:** React + TypeScript
-   **Сборщик:** Vite
-   **UI Компоненты:** shadcn/ui (для кастомизируемых, современных компонентов)
-   **Telegram SDK:** @telegram-apps/sdk (для получения `initData`, theme, viewport)
-   **Серверное состояние:** @tanstack/react-query (кеширование, refetch, invalidation после мутаций)
-   **UI состояние:** Zustand (для локального состояния: модалки, UI-флаги)

## 4. Пользовательский Интерфейс и Сценарии

### Навигация
-   Основная навигация осуществляется через **нижний TabBar**.
-   **Состав TabBar (слева направо):**
    1.  **🏆 Рейтинг**
    2.  **📋 Команды**
    3.  **🎯 Турниры**
    4.  **⚙️ Управление** (видима только для пользователей с ролями `Organizer` и `Admin`)

### Главный Экран (при запуске)
-   При запуске Mini App пользователь попадает на вкладку **"Рейтинг"**.
-   На каждой странице (вкладке) будет заголовок, обозначающий текущую страницу.

### Описание Вкладок

#### 1. 🏆 Рейтинг
-   Отображает список команд с их рейтинговыми показателями.
-   **Показатели:** `top_places` — количество первых мест; `avg_place` — среднее место команды по всем турнирам, где она участвовала (округление до 2 знаков).
-   **Период:** рейтинг считается за всё время.
-   **Сортировка:** Поддерживается сортировка по:
    -   Количеству первых мест (asc/desc)
    -   Среднему месту (asc/desc)
    -   **По умолчанию:** `top_places desc`, затем `avg_place asc`, затем `team_name asc` (tie-breaker).

#### 2. 📋 Команды
-   Отображает список всех зарегистрированных команд.
-   При клике на команду открывается страница с детальной информацией о ней (участники, история игр).
-   **Сортировка:** Поддерживается сортировка по:
    -   Названию команды (asc/desc)

#### 3. 🎯 Турниры
-   Отображает список всех проведённых и запланированных турниров.
-   При клике на турнир открывается страница с детальной информацией о нём (дата, место, результаты команд).
-   **Сортировка:** Поддерживается сортировка по:
    -   Названию турнира (asc/desc)
    -   Дате проведения (asc/desc)

#### 4. ⚙️ Управление (для `Organizer` и `Admin`)
-   Вкладка содержит функциональные кнопки, специфичные для ролей `Organizer` и `Admin`.

    -   **Для `Organizer`:**
        -   Кнопка "Создать команду" (форма для ввода названия команды).
        -   Кнопка "Добавить участника" (форма для выбора команды и ввода имени участника).
        -   Кнопка "Создать турнир" (форма для ввода названия, даты и места проведения турнира).
        -   Кнопка "Записать результат" (форма для выбора турнира, команды и указания занятого места).
        -   Кнопки "Редактировать" для команд, турниров и результатов.

    -   **Для `Admin` (дополнительно):**
        -   Кнопка "Назначить роли" (форма для выбора пользователя и назначения ему роли `viewer`, `organizer` или `admin`).

## 5. Спецификация REST API (Дополнение к существующему)

-   **Базовый путь:** `/api/v1`
-   **Сегменты доступа:**
    -   `/public` — доступен любому аутентифицированному пользователю (Viewer+).
    -   `/private` — доступен `Organizer`/`Admin`, а для управления ролями — только `Admin`.
-   **Роли в API:** `viewer`, `organizer`, `admin` (lower-case).
-   **Аутентификация:** Каждый запрос от Mini App должен содержать заголовок `Authorization: TMA <initData>`. Бэкенд валидирует `initData` (hash, bot token, `auth_date`) и извлекает `telegram_id` и роль. Для защиты от replay:
    -   `auth_date` должен быть в пределах TTL не более 1 часа.
    -   Для мутирующих запросов TTL не более 10 минут и кешировать `query_id` в Redis на 10 минут для блокировки повторов.
-   **Rate Limiting:** Ограничение запросов на уровне `telegram_id`:
    -   GET-запросы: 100 req/min
    -   POST/PATCH/DELETE: 20 req/min
    -   Хранение счётчиков в Redis с TTL 1 минута.
-   **Timeout:** Все запросы имеют timeout 15 секунд.
-   **Пагинация:** Все списки поддерживают `limit` (default 50, max 200) и `offset` (default 0). Ответ включает `meta`:
    ```json
    {
      "items": [],
      "meta": { "limit": 50, "offset": 0, "total": 123 }
    }
    ```
-   **Сортировка:** Все `GET` эндпоинты для списков поддерживают query-параметры `sort_by` и `order`.
-   **Удаление:** `DELETE` выполняет soft-delete (сохранение в БД с `deleted_at`, `deleted_by`). При удалении обязательно обновлять `updated_at` и `updated_by`. Удалённые сущности исключаются из публичных списков и из расчёта рейтинга; результаты турниров сохраняются для истории.
-   **Кеширование рейтинга:** Рейтинг команд кешируется в Redis с TTL 5 минут. Инвалидация при создании/изменении/удалении результатов.
-   **Optimistic Locking:** Для защиты от конкурентных изменений используется поле `version` (integer). При `PATCH`/`DELETE` клиент передаёт текущую версию, сервер проверяет совпадение и инкрементирует. При несовпадении — ошибка `409 Conflict`.
-   **Пользователи и участники:** `users` — авторизованные через Telegram пользователи; `members` — участники команд (не обязательно Telegram-аккаунты).
-   **Метаданные сущностей:** Все изменяемые сущности в БД должны иметь поля `created_at`, `updated_at`, `updated_by`. Поле `created_by` обязательно для `teams`, `members`, `tournaments`, `results`.
-   **Ограничения:** Для пары `tournament_id` + `team_id` допускается только один результат.

### Формат ошибок
Все ошибки возвращаются в `application/json`:
```json
{ "error": "текст ошибки" }
```
-   **Public-обёртка ошибок:** для `/api/v1/public/*` любые внутренние ошибки (5xx) маскируются и возвращаются как `{ "error": "internal_error" }` без деталей.
-   **Private-ответы:** также используют тот же формат, но сообщения могут быть более конкретными (например, `validation_error`, `forbidden`).

### Эндпоинты

**Public (Viewer+)**
-   `GET /api/v1/public/me` — Получить текущего пользователя и его роль.
-   `GET /api/v1/public/teams?sort_by=name&order=[asc|desc]` — Список команд.
-   `GET /api/v1/public/teams/{id}` — Базовая информация о команде.
-   `GET /api/v1/public/teams/{id}/members` — Участники команды (с пагинацией).
-   `GET /api/v1/public/teams/{id}/results` — История игр команды (с пагинацией).
-   `GET /api/v1/public/tournaments?sort_by=[name|date]&order=[asc|desc]` — Список турниров.
-   `GET /api/v1/public/tournaments/{id}` — Детали турнира.
-   `GET /api/v1/public/tournaments/{id}/results` — Результаты турнира (с пагинацией).
-   `GET /api/v1/public/rating?sort_by=[top_places|avg_place]&order=[asc|desc]` — Рейтинг команд (кешируется).

**Private (Organizer/Admin)**
-   `POST /api/v1/private/teams` — Создать команду.
-   `PATCH /api/v1/private/teams/{id}` — Редактировать команду.
-   `DELETE /api/v1/private/teams/{id}` — Удалить команду.
-   `POST /api/v1/private/teams/{id}/members` — Добавить участника.
-   `PATCH /api/v1/private/teams/{team_id}/members/{member_id}` — Редактировать участника.
-   `DELETE /api/v1/private/teams/{team_id}/members/{member_id}` — Удалить участника.
-   `POST /api/v1/private/tournaments` — Создать турнир.
-   `PATCH /api/v1/private/tournaments/{id}` — Редактировать турнир.
-   `DELETE /api/v1/private/tournaments/{id}` — Удалить турнир.
-   `POST /api/v1/private/tournaments/{id}/results` — Записать результат.
-   `PATCH /api/v1/private/tournaments/{id}/results/{result_id}` — Редактировать результат.
-   `DELETE /api/v1/private/tournaments/{id}/results/{result_id}` — Удалить результат.

**Private (Admin)**
-   `GET /api/v1/private/users` — Список пользователей с ролями.
-   `PUT /api/v1/private/users/{telegram_id}/role` — Изменить роль пользователя (тело: `{ "role": "organizer" }`).

---

## 6. Обработка Ошибок на Фронтенде

-   **Toast/Snackbar:** Все ошибки API отображаются через toast-уведомления (sonner или react-hot-toast).
-   **Сетевые ошибки:** При потере соединения — показывать banner "Нет подключения" с кнопкой retry.
-   **409 Conflict:** При конфликте версий — показывать диалог "Данные изменились. Обновить и повторить?"
-   **401 Unauthorized:** Редирект на повторную инициализацию через Telegram.
-   **Rate Limit (429):** Показывать "Слишком много запросов. Подождите."
-   **Retry-логика:** react-query автоматически ретраит failed запросы (3 попытки с exponential backoff).
-   **Loading states:** Скелетоны для списков, спиннеры для кнопок действий.

---

## 7. Deployment и Инфраструктура

-   **HTTPS:** Telegram Mini App требует HTTPS. Использовать Let's Encrypt или Cloudflare.
-   **CORS:** Не требуется, если API и фронтенд на одном домене. Если разные домены — настроить `Access-Control-Allow-Origin` для домена Mini App.
-   **Формат дат:** ISO 8601 (`2025-01-13T15:30:00Z`) для всех дат в API.
-   **Docker:** Добавить в `docker-compose.yml` сервис для сборки и отдачи фронтенда (или собирать в CI и копировать в Go-контейнер).
-   **ENV переменные для фронтенда:**
    -   `VITE_API_URL` — базовый URL API (для dev: `http://localhost:8080/api/v1`)

---

## 8. Тестирование

### Backend (Go) — обязательно
-   **Unit-тесты:** Покрытие ключевой логики:
    -   Валидация `initData` (парсинг, проверка hash, TTL)
    -   Middleware аутентификации и авторизации
    -   Rate limiting логика
    -   Optimistic locking (проверка version)
    -   Сортировка и пагинация
    -   Расчёт рейтинга (top_places, avg_place)
-   **Инструменты:** стандартный `testing`, `testify` для assertions, `sqlmock` или testcontainers для БД.
-   **Запуск:** `go test ./...` в CI и локально.
-   **Покрытие:** Стремиться к 70%+ для `internal/api`.

### Frontend — опционально (на будущее)
-   E2E тесты с Playwright при необходимости.

---

## 9. План Реализации (Высокоуровневый)

1.  **Этап 1: Бэкенд и структура.**
    -   Создать директорию `./frontend`.
    -   Реализовать в Go все REST эндпоинты `/api/v1` с сегментами `/public` и `/private`, включая middleware для аутентификации (`initData`) и логику сортировки/пагинации.
    -   Настроить Gin для отдачи статического `index.html` и SPA-fallback для клиентских роутов.

2.  **Этап 2: Фронтенд — "Режим чтения".**
    -   Инициализировать React-приложение в `./frontend` (Vite, shadcn, react-query, Zustand).
    -   Реализовать компонент `TabBar` и основную навигацию.
    -   Разработать страницы для вкладок "Рейтинг", "Команды", "Турниры".
    -   Интегрировать эти страницы с бэкендом (`/api/v1/public`) для получения и отображения данных, включая применение сортировки и пагинации.

3.  **Этап 3: Фронтенд — "Управление".**
    -   Разработать страницу для вкладки "Управление".
    -   Создать формы для всех действий `Organizer` и `Admin` (создание, изменение, удаление).
    -   Интегрировать эти формы с соответствующими `POST/PATCH/DELETE` эндпоинтами `/api/v1/private`.
    -   Реализовать логику авторизации на фронтенде для скрытия/отображения элементов UI в зависимости от роли пользователя.

---
## 10. Детальный План Реализации

### Этап 0: Миграции БД
- [ ] **Soft-delete поля:** Добавить миграцию для полей `deleted_at` (timestamp), `deleted_by` (bigint) в таблицы `teams`, `members`, `tournaments`, `results`.
- [ ] **Метаданные:** Добавить поля `created_by`, `updated_at`, `updated_by` где отсутствуют.
- [ ] **Optimistic locking:** Добавить поле `version` (integer, default 1) во все изменяемые таблицы.
- [ ] **Индексы:** Добавить индексы для `deleted_at IS NULL` (partial index) для оптимизации запросов.

### Этап 1: Бэкенд и Структура Проекта
- [ ] **Структура:** Создать директорию `./frontend` в корне проекта.
- [ ] **API Router:** Создать пакет `internal/api` с использованием Gin.
- [ ] **Аутентификация:** Реализовать Go-middleware для валидации `initData` из заголовка `Authorization: TMA <initData>`. Middleware должен извлекать данные пользователя и добавлять их в контекст запроса, включая проверку TTL (1 час для GET, 10 минут для мутаций).
- [ ] **Автосоздание пользователя:** При отсутствии пользователя в БД — создавать с ролью `viewer`.
- [ ] **Rate Limiting:** Реализовать middleware для rate limiting (100 req/min GET, 20 req/min mutations) с хранением счётчиков в Redis.
- [ ] **Replay Protection:** Кешировать `query_id` в Redis на 10 минут для блокировки повторных мутаций.
- [ ] **Эндпоинты (GET):** Реализовать все `GET` эндпоинты `/api/v1/public` с сортировкой и пагинацией на уровне запросов к базе данных.
- [ ] **Кеширование рейтинга:** Реализовать кеширование `/rating` в Redis с TTL 5 минут и инвалидацией при изменении результатов.
- [ ] **Эндпоинты (POST/PATCH/DELETE):** Реализовать все `POST`, `PATCH`, `DELETE` эндпоинты `/api/v1/private`. В каждом обработчике добавить проверку роли пользователя и optimistic locking.
- [ ] **Сервер для Frontend:** Настроить Gin для отдачи `index.html` и статических ассетов (JS, CSS) из директории `frontend/dist`.
    -   Добавить SPA-fallback на `index.html` для роутов вида `/teams/:id`.
- [ ] **Timeout:** Установить timeout 15 секунд для всех запросов.
- [ ] **Makefile:** Добавить команды в `Makefile` для удобной сборки фронтенда (`make build-frontend`) и для запуска бэкенд-сервера, обслуживающего API.
- [ ] **Unit-тесты:** Написать тесты для:
    -   Валидация `initData` (парсинг, hash, TTL)
    -   Middleware auth/authz
    -   Rate limiting
    -   Optimistic locking
    -   Расчёт рейтинга

### Этап 2: Фронтенд — "Режим Чтения"
- [ ] **Инициализация:** Инициализировать React-приложение в `./frontend` с помощью Vite + TypeScript.
- [ ] **Telegram SDK:** Установить `@telegram-apps/sdk`, настроить получение `initData`, theme, viewport.
- [ ] **Настройка UI:** Установить и настроить `shadcn/ui`.
- [ ] **Настройка State Management:** Установить `@tanstack/react-query` для серверного состояния и `Zustand` для UI-состояния. Настроить QueryClient с retry-логикой.
- [ ] **Навигация:** Установить `react-router-dom`. Настроить основной макет приложения с `TabBar` и роутинг для вкладок "Рейтинг", "Команды", "Турниры".
- [ ] **API Клиент:** Создать сервис-слой (`src/services/api.ts`) для инкапсуляции всех `fetch` запросов к бэкенду с передачей `initData` в заголовке.
- [ ] **Обработка ошибок:** Настроить глобальный error handler для toast-уведомлений (sonner). Обработка 409, 429, 401 ошибок.
- [ ] **Страница "Рейтинг":** Создать компонент страницы с `useQuery` для `/api/v1/public/rating`. Добавить элементы UI для выбора параметров сортировки.
- [ ] **Страница "Команды":** Создать компонент страницы с `useQuery` для `/api/v1/public/teams`. Добавить элементы UI для сортировки.
- [ ] **Страница "Турниры":** Создать компонент страницы с `useQuery` для `/api/v1/public/tournaments`. Добавить элементы UI для сортировки.
- [ ] **Страницы с детальной информацией:** Создать компоненты для отображения детальной информации о конкретной команде (`/teams/:id`) и турнире (`/tournaments/:id`) с отдельными запросами для members/results.
- [ ] **Loading/Error states:** Реализовать скелетоны для списков и обработку ошибок на каждой странице.

### Этап 3: Фронтенд — "Управление" и Функции для `Organizer`/`Admin`
- [ ] **Вкладка "Управление":** Создать компонент страницы "Управление", который будет виден только пользователям с соответствующими ролями (логику проверки роли взять из react-query `/me`).
- [ ] **Форма "Создать Команду":** Разработать компонент формы (модальное окно), используя `shadcn/ui`, и интегрировать с `useMutation` для `POST /api/v1/private/teams`.
- [ ] **Форма "Создать Турнир":** Аналогично реализовать форму для создания турнира с `useMutation` для `POST /api/v1/private/tournaments`.
- [ ] **Формы для участников и результатов:** Реализовать формы и логику для добавления участников и записи результатов турниров.
- [ ] **Редактирование:** Добавить формы редактирования команд, турниров и результатов. Передавать `version` в запросах `PATCH`. Обрабатывать 409 Conflict с предложением обновить данные.
- [ ] **Функционал удаления:** Добавить кнопки "Удалить" с модальным окном подтверждения. Передавать `version` в запросах `DELETE`.
- [ ] **Форма "Назначить Роли" (Admin):** Создать UI для администраторов, позволяющий просматривать список пользователей и изменять их роли, интегрировав его с `PUT /api/v1/private/users/{telegram_id}/role`.
- [ ] **Invalidation:** После успешных мутаций вызывать `queryClient.invalidateQueries()` для соответствующих ключей, чтобы обновить данные на страницах.
